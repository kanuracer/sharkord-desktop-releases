# Sharkord Desktop beta releases

Current beta: v0.5.20

## Downloads

Use the GitHub prerelease for binaries:

- https://github.com/kanuracer/sharkord-desktop-releases/releases/tag/v0.5.20

The beta updater reads `latest.json` on the `beta` branch.

## Latest hotfix

v0.5.20 fixes the Windows package so Sharkord Desktop starts as a normal GUI app instead of opening a Windows Terminal window with WebView2 log output.

See `latest.json` for updater metadata and `SHA256SUMS.txt` for checksums.
