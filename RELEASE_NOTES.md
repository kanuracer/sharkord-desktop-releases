# Sharkord Desktop 0.5.28 beta

Fixes members panel role grouping by using public roles from the join payload, removes the confusing channel-access-only checkbox, and avoids showing a fake “Ohne Rolle” group when role metadata is unavailable.
