# Sharkord Desktop v0.5.45 Beta

## Fixes

- Fixes Server Settings > Roles edit and delete actions so they open in-app dialogs instead of native browser prompt/confirm calls that can no-op in the desktop WebView.
- Adds a guarded delete flow for roles and keeps default/persistent roles protected.
- Adds regression coverage for role edit/delete buttons using app-owned dialogs.

Stable channel stays unchanged.
