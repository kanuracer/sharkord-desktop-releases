# Sharkord Desktop v0.5.18 beta

- Hide MFA/app-password settings on unsupported servers instead of showing broken `users.mfa.status` errors.
- Recommended Sharkord Server: `v0.0.23-kr.6` (`v0.0.23-kr.5` first introduced the account-security capability; `kr.6` fixes beta-aware server update latest-version display).
- Server beta `v0.0.23-kr.6` advertises `mfaAppPasswords` and fixes the webclient update panel so beta servers do not display fake latest version `0.0.0` or stale stable-only latest data.

## Assets

- Linux amd64: `sharkord-desktop-0.5.18-linux-amd64.tar.gz`
- Windows amd64: `sharkord-desktop-0.5.18-windows-amd64.exe`
- macOS arm64: `sharkord-desktop-0.5.18-darwin-arm64.zip`

See `SHA256SUMS.txt` for checksums.
